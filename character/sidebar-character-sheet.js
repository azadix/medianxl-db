/**
 * Planner sidebar: Overview / Stats / Config panes.
 * Tab switching is handled by {@link PlannerSidebarTabs.vue}.
 */

/** @deprecated No-op; kept for compatibility if anything still imports it. */
export function initSidebarViewTabs() {}
