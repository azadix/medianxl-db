/**
 * @file Character planner singleton facade (re-exports domain slices).
 * @module character/character-state
 *
 * Domain slices: planner-snapshot, planner-instance, planner-prereqs, planner-build-io,
 * planner-oskills, planner-quests, planner-allocation-checks, planner-core.
 */

export * from './planner-core.js';
